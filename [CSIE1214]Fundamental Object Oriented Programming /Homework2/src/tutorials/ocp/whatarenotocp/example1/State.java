package tutorials.ocp.whatarenotocp.example1;

/**
 * @author Waterball (johnny850807@gmail.com)
 */
// Violate OCP (An enum is never OCP)
// (Tips: do Polymorphism instead)
enum State {
    HAPPY, NORMAL, SAD
}
